name = "film-album-api"
main = "src/index.ts"
compatibility_date = "2025-03-01"

[vars]
# 图床配置
IMG_BED_URL = "${IMG_BED_URL}"
# NOTE: 生产环境中应使用 wrangler secret 设置以下密钥
# JWT_SECRET = "your-jwt-secret"
IMG_BED_TOKEN = "${IMG_BED_TOKEN}"

[[d1_databases]]
binding = "DB"
database_name = "film-album-db"
database_id = "placeholder-replace-after-creation"
